# Explicit Cyan Button Class Fix
- Adds direct high-specificity rules for registry-add, asset-add, new-device and Safety Twin mode buttons.
- Forces dark navy text for normal, visited and hover states.
- Advances the stylesheet cache key to v3 so browsers and Render do not reuse the prior CSS response.
- Builds cumulatively on the final device workflow, Quick Calibration, board profiles and centralized Data Management.
