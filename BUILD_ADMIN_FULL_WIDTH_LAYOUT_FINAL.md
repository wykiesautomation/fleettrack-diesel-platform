# Platform Administration Full-Width Layout Final
- Replaces the conflicting nested `.side` admin class with isolated `admin-shell`, `admin-nav` and `admin-content` classes.
- Applies one shared full-width layout to Overview, Customers, Users, Subscriptions, Payments, Invoices, Devices, Support, Audit Log and Platform Settings.
- Admin panels and tables now use the complete available main area.
- Search and filter controls flow horizontally on desktop and stack on mobile.
- Wide tables remain readable through panel-contained horizontal scrolling only.
- The admin navigation becomes a horizontal scrollable tab row on tablets and phones.
- Loads after all customer and page CSS with cache version v4.
- Preserves the cumulative device selector, board pin profiles, Quick Calibration, Data Management and explicit cyan-button contrast fixes.
