services:
  - type: worker
    name: assettrack360-mqtt-worker
    runtime: python
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: python mqtt_worker.py
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: assettrack360-db
          property: connectionString
