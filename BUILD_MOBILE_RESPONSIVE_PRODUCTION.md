# Mobile Responsive Production Pass

Responsive support added for common Android and iPhone browser widths. Navigation becomes a drawer with backdrop and scroll lock, header actions simplify, tank inventory stacks, output buttons become large touch controls, cards collapse safely, and wide tables retain controlled horizontal scrolling. Backend, telemetry, calibration, device commands and safety interlocks are unchanged.
