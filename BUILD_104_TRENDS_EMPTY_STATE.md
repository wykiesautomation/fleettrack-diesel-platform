# Build 104 Trends & Limits Empty State

The no-device/no-assignment state now uses the compact REV28 layout: title, assigned-points status, a two-column device/point context card, one progress row, and one concise guidance card. The oversized disabled tabs and vertical input form were removed from the empty state. The full four-tab editor still appears automatically when a real signal is assigned.
