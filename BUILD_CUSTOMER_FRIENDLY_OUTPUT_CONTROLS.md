# Customer-friendly output controls

Output controls now use plain commands and clear confirmed states. A click immediately shows COMMAND SENT, while the large OUTPUT ON/OFF badge changes only after firmware feedback confirms the state. The active state button is disabled to prevent duplicate commands. Existing Local Arm, simulation lockout, online checks and fresh-feedback safety remain authoritative.
