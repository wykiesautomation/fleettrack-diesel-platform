# AssetTrack 360 REV28 Shell and Navigation CSS Fix

- Adds a dedicated shared shell stylesheet loaded after global and page CSS.
- Restores the fixed sidebar, brand, workspace card, section labels, navigation links, top header and responsive mobile drawer.
- Adds explicit cache versions for the shared shell and Fleet Overview stylesheet.
- Does not change routes, customer data, PostgreSQL, telemetry, devices, outputs, calibration or authentication.
